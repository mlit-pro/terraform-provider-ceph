---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ceph_cluster_user Resource - Ceph"
subcategory: ""
description: |-
  Manages a CephX cluster user (a ceph auth entity) and its capabilities.
---

# ceph_cluster_user (Resource)

Manages a CephX cluster user (a `ceph auth` entity) and its capabilities.

## Example Usage

```terraform
resource "ceph_cluster_user" "rbd" {
  entity = "client.rbd"
  capabilities = {
    mon = "profile rbd"
    osd = "profile rbd pool=rbd"
  }
}

output "rbd_keyring" {
  value     = ceph_cluster_user.rbd.keyring
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `capabilities` (Map of String) Capabilities keyed by daemon type, e.g. `{ mon = "allow r", osd = "allow rwx pool=rbd" }`. Updates overwrite all existing capabilities.
- `entity` (String) CephX entity name, e.g. `client.terraform`. Changing this forces a new user.

### Read-Only

- `key` (String, Sensitive) CephX secret key for the entity.
- `keyring` (String, Sensitive) Full keyring text for the entity, suitable for writing to a keyring file.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# CephX cluster users are imported by their entity name.
terraform import ceph_cluster_user.rbd client.rbd
```
